# 项目协作约定

- 每次完成一组源码、资源或配置修改后，必须立即使用 Git 创建提交；提交前检查 `git status`，提交后确认工作区干净。
- “天下”页保留旧版明代地图的范围、时间轴、事件卡片和右侧图层入口；顶部旧版“明代／现代”分段按钮已移除。
- 右侧图层菜单当前的“现代区划”仅为临时半透明叠加，尚未实现真实的现代地图数据与边界对照。后续应以保持明代地图不变为前提，接入准确的现代地图图层，再替换该临时实现。
- 人物详情的姓名下方只保留官职、年号与年份这一行，不另设“基本资料”。详情正文固定为“生平（含教育背景）”“家族与子嗣”“资料状态”；“时代背景”“人物关系”“相关事件”不在详情页内展示，待后续以独立入口承载。
